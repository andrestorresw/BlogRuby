class HomeController < ApplicationController
  def index
    #Aun estando vacia rails busca en la carpeta views una subcarpeta en views llamada igual que el controlador y una archivo que se llame igual que la accion
  end
end
